# Dependencias del TABLERO (la app Streamlit). Livianas a propósito: solo lo
# que la app importa. El entorno completo del análisis está en
# requirements-analisis.txt.
#
# RESTRICCIÓN NO NEGOCIABLE: scikit-learn debe ser IDÉNTICO aquí y en
# requirements-analisis.txt (1.9.0); de esa versión depende que los modelos
# de models/ carguen. Desplegar con Python 3.13 (ver README).

streamlit==1.63.0
scikit-learn==1.9.0
pandas==2.3.3
numpy==2.5.1
joblib==1.5.3
shap==0.52.0
